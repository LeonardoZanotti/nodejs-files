# NodeJS Files
Guia para commitar

```
Clone o repositório usando esse comando
$ git clone https://github.com/LeonardoZanotti/nodejs-files.git

Pegar alterações do remoto antes de começar a trabalhar
$ git pull

Mandar suas alterações pro remoto
$ git add .
$ git commit -m "mensagem do commit"
$ git push
```